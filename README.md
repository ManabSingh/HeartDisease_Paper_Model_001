# HeartDisease_Paper_Model_001

## Branches

As of the initial setup, this repository has **2** branches:

| Branch | Description |
|--------|-------------|
| `main` | Default branch |
| `copilot/fix-branch-issues` | Working branch for fixes |

> **Note:** Run `git branch -a` to see the current list of branches.
